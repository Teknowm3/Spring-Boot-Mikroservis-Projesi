# Auth Service Configuration
spring.application.name=auth-service
server.port=8081

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/authdb?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Eureka Client
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# JWT Configuration
jwt.secret=${JWT_SECRET:yazilimsistemtasarim2024secretkeythisisasupersecretkey123456789}
jwt.expiration=86400000

internal.signature.secret=${INTERNAL_SIGNATURE_SECRET:W8fXc2qA9tRk7nV4pL1sD6mZ0yH3uJ5bQ2eG7xN9cK4vS1aT}
user-service.base-url=${USER_SERVICE_BASE_URL:http://localhost:8082}

admin.seed.username=${ADMIN_SEED_USERNAME:}
admin.seed.password=${ADMIN_SEED_PASSWORD:}
admin.seed.email=${ADMIN_SEED_EMAIL:}

# Actuator
management.endpoints.web.exposure.include=health,info,prometheus

# Metrics
management.metrics.distribution.percentiles-histogram.http.server.requests=true
